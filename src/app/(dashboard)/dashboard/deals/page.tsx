import { ComingSoon } from "@/components/dashboard/coming-soon";

export default function DealsPage() {
  return (
    <ComingSoon
      title="Deals"
      description="Kanban pipeline from lead to closed — drag and drop across stages."
    />
  );
}
